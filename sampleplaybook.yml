# This is just a sample playbook
---
- hosts: all
  become: yes
  tasks:
  - name: install nginx
    apt:
      name: nginx
      state: present
  - name: Start nginx
    service:
      name: nginx
      state: started


